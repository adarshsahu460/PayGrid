{
  "name": "paygrid-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "services/*",
    "common/*"
  ],
  "scripts": {
    "bootstrap": "npm install --workspaces",
    "lint": "eslint . --ext .ts,.js",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^20.17.50",
    "eslint": "^8.0.0",
    "prettier": "^2.8.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "kafkajs": "^2.2.4"
  }
}
